/**
 * Storage configuration constants
 * Default values for file storage operations
 */

/**
 * Default image quality (0-100) for image compression
 */
export const DEFAULT_IMAGE_QUALITY = 60;

/**
 * Minimum image quality value
 */
export const MIN_IMAGE_QUALITY = 0;

/**
 * Maximum image quality value
 */
export const MAX_IMAGE_QUALITY = 100;
