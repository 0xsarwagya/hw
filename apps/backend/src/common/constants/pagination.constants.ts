/**
 * Pagination configuration constants
 * Default values for pagination across the application
 */

/**
 * Default number of items per page
 */
export const DEFAULT_PAGE_SIZE = 10;

/**
 * Maximum number of items per page
 */
export const MAX_PAGE_SIZE = 100;

/**
 * Maximum number of keys/files that can be returned in a single list operation
 */
export const MAX_LIST_KEYS = 1000;

/**
 * Default number of keys/files returned in list operations
 */
export const DEFAULT_LIST_KEYS = 100;

