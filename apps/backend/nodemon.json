{
  "watch": ["src"],
  "ext": "ts",
  "ignore": ["src/**/*.spec.ts", "node_modules", "dist"],
  "exec": "pnpm run build && node dist/main.js",
  "env": {
    "NODE_ENV": "development"
  },
  "delay": 1000
}
